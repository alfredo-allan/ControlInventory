{
    "version": 2,
    "buildCommand": "pnpm run build:client",
    "outputDirectory": "dist/public",
    "installCommand": "pnpm install",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}