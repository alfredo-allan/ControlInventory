{
    "version": 2,
    "installCommand": "pnpm install",
    "buildCommand": "pnpm run build:front",
    "outputDirectory": "client/dist",
    "framework": "vite",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}