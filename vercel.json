{
    "version": 2,
    "buildCommand": "pnpm run build:client",
    "outputDirectory": "client/dist",
    "installCommand": "pnpm install",
    "framework": "vite",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/client/dist/$1"
        }
    ]
}